{#
/**
* @file
* Default theme implementation to display a taxonomy term.
*
* Available variables:
* - url: URL of the current term.
* - name: (optional) Name of the current term.
* - content: Items for the content of the term (fields and description).
* Use 'content' to print them all, or print a subset such as
* 'content.description'. Use the following code to exclude the
* printing of a given child element:
* @code
* {{ content|without('description') }}
* @endcode
* - attributes: HTML attributes for the wrapper.
* - page: Flag for the full page state.
* - term: The taxonomy term entity, including:
* - id: The ID of the taxonomy term.
* - bundle: Machine name of the current vocabulary.
* - view_mode: View mode, e.g. 'full', 'teaser', etc.
*
* @see template_preprocess_taxonomy_term()
*
* @ingroup themeable
*/
#}
<div class="accordion-item"
  style="background-image:url('{{ file_url(term.field_imagen_principal_mobile.entity.field_media_image.entity.fileuri) }}')">
  <h2 class="accordion-header" id="flush-heading-{{term.id}}"><button aria-controls="flush-collapse-{{term.id}}" aria-expanded="false"
      class="accordion-button collapsed" data-bs-target="#flush-collapse-{{term.id}}" data-bs-toggle="collapse" type="button">{{
      name }}<p class="card-subtitle" title="{{ term.field_subtitulo.value }}">{{ term.field_subtitulo.value }}</p>
      </button></h2>

  <div aria-labelledby="flush-heading-{{term.id}}" class="accordion-collapse collapse" data-bs-parent="#accordion-modal"
    id="flush-collapse-{{term.id}}">
    <div class="accordion-body">

      <p>{{content.field_resumen}}</p>
      {% if term.id == 121 %}
      <a class="btnCap" href="https://archivo.comisiondelaverdad.co/" title="{{ name }}">Más...</a>
      {% elseif term.id == 153 %}
      <a class="btnCap" href="https://comisiondelaverdad.co/recorridos/como-lo-hicimos" title="{{ name }}">Más...</a>
      {% else %}
      <a class="btnCap" href="{{ url }}" title="{{ name }}">Más...</a>
      {% endif %}
    </div>
  </div>
</div>