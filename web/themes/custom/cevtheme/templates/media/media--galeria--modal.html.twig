{#
/**
* @file
* Default theme implementation to present a media item.
*
* Available variables:
* - media: The media item, with limited access to object properties and
* methods. Only method names starting with "get", "has", or "is" and
* a few common methods such as "id", "label", and "bundle" are available.
* For example:
* - entity.getEntityTypeId() will return the entity type ID.
* - entity.hasField('field_example') returns TRUE if the entity includes
* field_example. (This does not indicate the presence of a value in this
* field.)
* Calling other methods, such as entity.delete(), will result in
* an exception.
* See \Drupal\Core\Entity\EntityInterface for a full list of methods.
* - name: Name of the media item.
* - content: Media content.
* - title_prefix: Additional output populated by modules, intended to be
* displayed in front of the main title tag that appears in the template.
* - title_suffix: Additional output populated by modules, intended to be
* displayed after the main title tag that appears in the template.
* - view_mode: View mode; for example, "teaser" or "full".
* - attributes: HTML attributes for the containing element.
* - title_attributes: Same as attributes, except applied to the main title
* tag that appears in the template.
*
* @see template_preprocess_media()
*
* @ingroup themeable
*/
#}

{% if content.field_visualizacion.0["#markup"] == 2 %}
<div id="carousel-gallery-polaroids-static-modal-{{media.id}}" class="carousel slide" data-bs-ride="carousel">
  <div class="carousel-indicators" style="bottom: 112px;">
    {% for item in image_galeria_url %}
    <button type="button" data-bs-target="#carousel-gallery-polaroids-static-modal-{{media.id}}" data-bs-slide-to="{{ loop.index0 }}"
      class="{{ loop.index0 == 0 ? 'active' : '' }}" aria-current="true" aria-label="Slide 1"
      style="width: 8px; height: 8px;border-radius: 100%"></button>
    {% endfor %}
  </div>
  <div class="position-absolute w-100">
    <img src="/themes/custom/cevtheme/img/polaroid-marco.png" style="width: 100%;" alt="">
  </div>
  <div class="carousel-inner carousel-inner-polaroids">
    {{ content.field_media_image }}
  </div>
  <button class="carousel-control-prev" type="button" data-bs-target="#carousel-gallery-polaroids-static-modal-{{media.id}}"
    data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
  </button>
  <button class="carousel-control-next" type="button" data-bs-target="#carousel-gallery-polaroids-static-modal-{{media.id}}"
    data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
  </button>
</div>

{% elseif content.field_visualizacion.0["#markup"] == 3 %}
<div id="carousel-gallery-polaroids-modal-{{media.id}}" class="carousel slide carousel-fade carousel-with-polaroid"
  data-bs-ride="carousel">
  <div class="carousel-indicators">
    {% for item in image_galeria_url %}
    <button type="button" data-bs-target="#carousel-gallery-polaroids-modal-{{media.id}}" data-bs-slide-to="{{ loop.index0 }}"
      class="{{ loop.index0 == 0 ? 'active' : '' }}" aria-current="true" aria-label="Slide 1"
      style="width: 8px; height: 8px;border-radius: 100%"></button>
    {% endfor %}
  </div>
  <div class="position-absolute w-100">
    <img src="/themes/custom/cevtheme/img/polaroid-marco.png" style="width: 100%;" alt="">
  </div>
  <div class="carousel-inner carousel-inner-polaroids">
    {{ content.field_media_image }}
  </div>
  <button class="carousel-control-prev" type="button" data-bs-target="#carousel-gallery-polaroids-modal-{{media.id}}"
    data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
  </button>
  <button class="carousel-control-next" type="button" data-bs-target="#carousel-gallery-polaroids-modal-{{media.id}}"
    data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
  </button>
</div>
{% else %}
<div id="carousel-gallery-modal-{{media.id}}" class="carousel slide" data-bs-ride="carousel" style="margin-top:20px">
  <div class="carousel-indicators" style="bottom: 0px;">
    {% for item in image_galeria_url %}
    <button type="button" data-bs-target="#carousel-gallery-modal-{{media.id}}" data-bs-slide-to="{{ loop.index0 }}"
      class="{{ loop.index0 == 0 ? 'active' : '' }}" aria-current="true" aria-label="Slide 1"
      style="width: 8px; height: 8px;border-radius: 100%"></button>
    {% endfor %}
  </div>
  <div class="carousel-inner">
    {{ content.field_media_image }}
  </div>
  <button class="carousel-control-prev" type="button" data-bs-target="#carousel-gallery-modal-{{media.id}}" data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
  </button>
  <button class="carousel-control-next" type="button" data-bs-target="#carousel-gallery-modal-{{media.id}}" data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
  </button>
</div>

{% endif %}